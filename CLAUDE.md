# CLAUDE.md

The full agent guide for this project lives in **AGENTS.md** — read it before working. It covers the
architecture, where to write code (`src/modules/<feature>/`), what never to touch
(`src/generated.*.ts`), conventions, and the commands to run.

@AGENTS.md
